# 하이브리드 로그 시스템을 위한 필수 패키지

# 웹 서버 및 API
flask>=2.3.0
flask-cors>=4.0.0

# 작업 스케줄링  
apscheduler>=3.10.0

# 시스템 모니터링
psutil>=5.9.0

# 데이터 처리 및 압축
pandas>=2.0.0
numpy>=1.24.0

# 기본 패키지 (대부분 이미 설치되어 있음)
colorama>=0.4.6
argparse  # Python 3.2+ 기본 포함
logging   # Python 기본 포함  
threading # Python 기본 포함
json      # Python 기본 포함
gzip      # Python 기본 포함
pathlib   # Python 3.4+ 기본 포함
time      # Python 기본 포함
datetime  # Python 기본 포함
collections  # Python 기본 포함

# 선택적 패키지 (기존 시스템 호환성)
scapy>=2.5.0
joblib>=1.3.0

# 설정 파일 관리 (P1 개선)
pyyaml>=6.0

# 테스트 프레임워크 (P1 개선)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# 머신러닝 (기존)
scikit-learn>=1.3.0
torch>=2.0.0

